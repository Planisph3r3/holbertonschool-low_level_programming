#include <stdio.h>

/**
 * Description: Write on screen using puts()
 *
 * Return: Zero on success
*/

int main(void)
{
int word = "Programming is like building a multilingual puzzle\n";
puts(word);
return (0);
}
