#include <stdio.h>

/**
 * main - Starting program
 *
 * Description: Printing a text to StdOut using printf() function
 *
 * Return: Zero equals success
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
