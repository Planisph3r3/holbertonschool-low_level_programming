#include "main.h"
/**
 * _abs - gives the absolute value of a number
 *
 *@a: value to be compared
 *
 * Return: a or -a value
 */
int _abs(int a)
{
if (a > 0)
{
return (a);
}
else
{
return (-a);
}
}
