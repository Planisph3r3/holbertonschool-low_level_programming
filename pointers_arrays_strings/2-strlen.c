#include <stdio.h>
/**
 * _strlen - checks for how many characters a string have
 *
 *@s: pointer of str value
 *
 * Return: 'a' value
 */

int _strlen(char *s)
{
int a;
for (a = 0; s[a] != '\0'; a++)
{

}
return (a);
}
